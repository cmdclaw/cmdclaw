{
  "name": "@cmdclaw/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file=../web/.env index.ts",
    "dev:selfhost": "bun --watch --env-file=../web/.env.selfhost index.ts",
    "start": "bun --env-file=../web/.env index.ts",
    "bundle": "rm -rf dist && bun build index.ts --target bun --outdir dist --external cpu-features",
    "build": "bunx tsc --noEmit -p tsconfig.json",
    "check": "bunx tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cmdclaw/core": "workspace:*",
    "@cmdclaw/db": "workspace:*"
  }
}
