[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile.app"

[deploy]
startCommand = "bun run --silent start -- -H 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
